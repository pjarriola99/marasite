<h1>Listing pictures</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pictures.each do |picture| %>
  <tr>
    <td><%= picture.title %></td>
    <td><%= image_tag picture.url, :size => "200x200" %></td>
    <td><%= link_to 'Show', picture %></td>
    <td><%= link_to 'Edit', edit_picture_path(picture) %></td>
   
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Picture', new_picture_path %>
